services:
  backend:
    build:
      context: ./backend # Ruta a la carpeta del back
      target: development # Etapa del Dockerfile
    image: adm-taxi-backend:latest

  frontend:
    build:
      context: ./frontend # Ruta a la carpeta del front
      target: development
    image: adm-taxi-frontend:latest